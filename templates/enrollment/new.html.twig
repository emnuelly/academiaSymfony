{% extends 'base.html.twig' %}
{% form_theme claudio 'bootstrap_4_layout.html.twig' %}
{% block title %}Matricula {% endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-12">
            <h1>Matricular Aluno</h1>
        </div>
        <div class="col-md-6">
            {{ form_start(claudio) }}
                {{ form_widget(claudio) }}
            <div class="form-group">
                <button type="submit" class="btn btn-primary">
                    <i class="fa fa-save"></i> Salvar
                </button>
            </div>
            {{ form_end(claudio) }}
        </div>


    </div>
{% endblock %}